info:
  name: Create Tasks
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3333/tasks
  body:
    type: json
    data: |-
      {
        "title": "Tarefa teste02",
        "description": "So pra ver se esta funcionando mesmo!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
