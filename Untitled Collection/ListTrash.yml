info:
  name: ListTrash
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3333/tasks/trash
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
